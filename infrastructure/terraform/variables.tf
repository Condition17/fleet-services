variable "project_id" {
  type    = string
  default = "GCP project id."
}

variable "region" {
  type    = string
  default = "Project's region in GCP."
}

variable "zone" {
  type    = string
  default = "Project's speciffic zone in GCP."
}
